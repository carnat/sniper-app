> ⚠️ **DEPRECATED** — This file is superseded by `docs/doctrine/` and the CLAUDE.md hierarchy.
> Do not use for new sessions. Retained for historical reference only.
> Authoritative doctrine: `docs/doctrine/` | Context: root `CLAUDE.md` + subfolder `CLAUDE.md` files.

# SNIPER TOOLS — CLAUDE CODE HANDOFF

**Date:** Apr 3, 2026 (overhauled from Mar 16, 2026 original)
**From:** Sniper Spinoff Project (claude.ai)
**To:** Claude Code — sniper-app repo

---

## WHAT YOU ARE BUILDING

A personal investment command center for the Sniper Doctrine system. Two tools in one repo:
1. `command_center/index.html` — static offline dashboard (6 tabs), doctrine-aware gate status, daily situational awareness
2. `streamlit_app.py` — full portfolio OS (transactions, analytics, tax lots, options greeks) — business logic extracted to `sniper/` package

**Non-negotiable constraint:** No feature may auto-execute trades, auto-send orders, or remove Commander from the decision chain. Alert systems are informational only.

---

## REPO STRUCTURE (carnat/sniper-app)

```
carnat/sniper-app/
├── CLAUDE.md                     ← root context + constraints
├── streamlit_app.py              ← Streamlit UI layer (~3,827 lines, delegates to sniper/)
├── requirements.txt              ← streamlit, yfinance, pandas, ratelimit, requests, cryptography
├── VERSION                       ← semver (currently 1.3.0)
├── sniper/                       ← extracted business logic package (Session 14)
│   ├── __init__.py
│   ├── models.py                 ← PriceSnapshot, Position, Transaction, GateStatus, FundPosition
│   ├── version.py
│   ├── persistence.py            ← 7 load/save pairs for file I/O
│   ├── options.py                ← Black-Scholes Greeks, IV rank/percentile
│   ├── tax_lots.py               ← SQLite FIFO/LIFO/AVERAGE lot tracking
│   ├── csv_import.py             ← Yahoo CSV parsing
│   ├── market.py                 ← yfinance quotes, fundamentals, options chains
│   ├── funds.py                  ← SEC Thailand API, fund registry, NAV fetch
│   ├── news.py                   ← Finnhub news, symbol resolution
│   └── alerts.py                 ← Price alert state machine with cooldown/dedup
├── scripts/
│   ├── CLAUDE.md                 ← pipeline context
│   ├── secret_scan.py
│   ├── fetch_prices.py           ← writes data/prices.json (doctrine_ops v1.15.1 tickers)
│   ├── encrypt_private.py        ← encrypts private.json → private.enc.json (AES-GCM)
│   ├── import_portfolio.py       ← imports data/portfolio_config.json → data/portfolio.json
│   └── claude_guardrail.py       ← pre-tool hook safety check
├── tests/
│   ├── test_market_helpers.py
│   ├── test_user_expert.py
│   ├── test_user_beginner.py
│   └── test_secret_scan.py
├── data/
│   ├── prices.json               ← GitHub Action output (enriched schema)
│   ├── portfolio_config.json     ← GITIGNORED — Commander-filled portfolio config
│   └── portfolio.json            ← GITIGNORED — generated by import_portfolio.py
├── docs/
│   ├── sniper_claude_code_handoff.md  ← this file
│   └── doctrine/                 ← authoritative doctrine rules (public, committed)
│       ├── CLAUDE.md
│       ├── README.md
│       ├── doctrine_core.md          ← v1.12.1
│       ├── doctrine_ops.md           ← v1.15.1
│       ├── doctrine_council.md       ← v1.11.0
│       ├── doctrine_pe.md            ← v1.6.0
│       ├── doctrine_core_tripwires.md ← v1.0.0-BETA.1
│       └── doctrine_core_matrix.md   ← v1.0.0-BETA.1
├── .github/workflows/
│   ├── ci.yml                    ← CI tests
│   ├── update_prices.yml         ← daily yfinance fetch (09:00 BKK)
│   └── pages.yml                 ← GitHub Pages deploy
├── .claude/
│   └── settings.json             ← hooks config (Commander-in-loop enforcement)
├── command_center/
│   ├── CLAUDE.md                 ← static HTML only, no Python, no server
│   ├── index.html                ← Command Center v2 (~3,998 lines, 6 tabs)
│   ├── private.json              ← GITIGNORED — local dev plaintext private data
│   └── private.enc.json          ← AES-GCM encrypted — safe to commit, PIN-decrypted in browser
├── .sniper-plugin/
│   ├── .claude-plugin/
│   │   └── plugin.json           ← manifest
│   ├── .mcp.json                 ← Yahoo Finance MCP
│   ├── commands/                 ← 4 slash commands (scorecard, thesis, brief, startup)
│   └── skills/                   ← 13 compressed doctrine summary files
├── index.html                    ← root redirect → ./command_center/
└── .streamlit/
    └── secrets.toml              ← GITIGNORED — local only, NEVER commit
```

---

## TICKER MAP (doctrine_ops v1.15.1)

| Group | Tickers | Count |
|-------|---------|-------|
| Arsenal Core | VRT, ASTS, VST, MU, APH, ANET, TSM, ONDS, FN, COHR | 10 |
| Watchtower Core | TSEM, BWXT, MOD, NBIS, FORM, ENTG, ONTO, LITE, QRVO, PLTR, KTOS, SKYT | 12 |
| Watchtower Satellite | RKLB, SATL, PL | 3 |
| Benchmarks | SOXX, VOO, QQQ, EWY | 4 |
| Macro | ^VIX, THB=X, SPY | 3 |

---

## PRIVATE DATA (PIN-ENCRYPTED)

Public page shows: prices, P&L %, gate status, thesis dates.
Hidden by default: shares count, ₿ values, vault THB totals.

**Local dev:** `command_center/private.json` (gitignored) — loaded without PIN
**Production:** `command_center/private.enc.json` (committed) — PIN modal on load

State data (positions, shares, cost bases) is NOT hardcoded in `index.html` — it lives in:
- `command_center/private.json` / `private.enc.json` (for Command Center display)
- `data/portfolio_config.json` → `data/portfolio.json` (for portfolio import pipeline)

To re-encrypt after updating `private.json`:
```bash
python scripts/encrypt_private.py
git add command_center/private.enc.json
git commit -m "chore: update encrypted private data"
git push
```

---

## data/prices.json SCHEMA (v1.15.1 enriched)

```json
{
  "last_updated": "2026-04-03T02:00:00Z",
  "fx_rate": 33.45,
  "vix": 22.5,
  "spy_above_200dma": true,
  "regime": "YELLOW",
  "prices": {
    "VRT": {
      "price": 280.50,
      "change": 2.15,
      "change5d": [278.0, 275.5, 279.0, 281.0, 280.5],
      "adv": 1250000,
      "volume": 980000,
      "high52w": 310.00,
      "low52w": 180.00,
      "sma50": 270.00,
      "sma200": 255.00,
      "market_time": "2026-04-02T20:00:00Z",
      "news": [
        { "title": "...", "link": "...", "publisher": "...", "providerPublishTime": 1711900000 }
      ]
    }
  },
  "gate": { "vix_freeze": false, "vix": 22.5, "thb_zone": "B", "thb": 33.45 },
  "adv": { "VRT": 1250000 },
  "news": { "VRT": [...] }
}
```

Backward-compatible keys (`gate`, top-level `adv` dict, top-level `news` dict) still present for older code paths.

---

## DOCTRINE RULES (gate logic reflected in UI)

**VIX Gate (DC-15, doctrine_ops v1.15.1):**
- VIX < 22 → GREEN → full deployment allowed
- VIX 22–<25 → YELLOW → deploy with caution
- VIX 25–≤30 → ORANGE (FREEZE) → all deployment blocked
- VIX > 30 → RED (HARD FREEZE) → all deployment blocked + emergency protocols

**THB/USD FX Zones:**
- < 32.00 → Zone A (cyan) → deploy full ammo immediately
- 32.00–36.00 → Zone B (green) → normal deployment
- > 36.00 → Zone C (amber) → split 50/50 deploy/hold

**DC-06 Neglect Clocks:**
- Day 30 → ⚠ NEGLECT flag
- Day 60 → forced /council trim (red, blinking)

---

## COMMAND CENTER FEATURES (6 tabs)

| Tab | ID | Key features |
|-----|----|-------------|
| ARSENAL + STATUS | `arsenal` | Position table, P&L, PRI gauge, signal convergence alerts |
| VAULT HEALTH | `vault` | Fund positions, vault NAV, rebalance indicators |
| WATCHTOWER | `watchtower` | WT slots, blackout calendar, SOs, ADV gates |
| NEWS | `news` | Filterable feed (ALL/ARSENAL/WATCHTOWER), sentiment badges |
| BACKTESTER | `backtester` | DCA simulation, Matrix vs equal-weight allocation |
| WAR ROOM | `warroom` | Canvas 2D pixel art, 28 stock agents + 3 Triad Council agents |

**v2 Enhancements (CMD-6→11):**
- PRI Gauge — SVG circular 0–100 composite from 5 risk factors
- Signal Convergence — 7 cross-signal detection rules
- Inline Sparklines — 28×12px SVG from change5d data
- Ticker Tape — auto-scrolling price bar below header
- News Sentiment Badges — keyword-based BULLISH/BEARISH classification
- Keyboard Shortcuts — 1–6 tabs, R refresh, ? help, Esc close
- Auto-Refresh — 5-min polling with localStorage persistence
- Mobile Bottom Tab Bar — fixed bottom nav at ≤600px
- PanelManager — collapse/expand with localStorage state

**War Room (CMD-4 + CMD-5):**
- Canvas 2D rendering engine with requestAnimationFrame game loop
- 28 stock agents + VIX Sentinel + Commander + 3 Triad Council agents
- Sector palettes: semi, defense, space, network, energy, vault, cmd, sentinel, doctrine, gate, watcher
- Triad Council: DOCTRINE (purple), GATE (steel-blue), WATCHER (emerald) — continuously audit doctrine
- Consensus detection: when 2-of-3 Triad flag same ticker, ⚡ CONSENSUS label

---

## SESSION ORDER

| Session | Task | Key output | Status |
|---------|------|------------|--------|
| 0 | CLAUDE.md hierarchy + hooks + guardrail + plugin scaffold | Root + subfolders CLAUDE.md, `.claude/settings.json`, `claude_guardrail.py`, `.sniper-plugin/` skeleton | ✅ Complete |
| 1 | Price pipeline + wire Command Center | `fetch_prices.py`, `update_prices.yml`, `index.html` reads `data/prices.json` | ✅ Complete |
| 1b | Privacy layer + PIN encryption | `private.enc.json`, `encrypt_private.py`, PIN modal in `index.html` | ✅ Complete |
| 2 | Streamlit restyle + plugin skills population | CSS block replaced, `skills/` files populated | ✅ Complete |
| 3 | CMD-2 Watchtower panel + ADV/news pipeline + price pipeline upgrade | 3rd tab + NEWS tab; `fetch_prices.py` doctrine v1.15.1 tickers + enriched data (SMA, 52w, regime) | ✅ Complete |
| 4 | CMD-3 Live Backtester panel | Backtester tab — DCA simulation, Matrix allocation vs equal-weight | ✅ Complete |
| 4b | CMD-4 War Room — Agent-Based Pixel Art | War Room tab — Canvas 2D game engine, 28 pixel agents, regime effects, speech bubbles | ✅ Complete |
| 5 | CMD-5 Triad Council — Doctrine Audit Overlay | 3 council agents (DOCTRINE/GATE/WATCHER) in War Room; audit engine, consensus, tooltips | ✅ Complete |
| 6+ | P7 alert pipeline | Post-Level 1 (฿800K trigger) | 🔜 Not started |
| 7 | Doctrine integration + portfolio import | `docs/doctrine/`, `scripts/import_portfolio.py`, portfolio config schema | ✅ Complete |
| 8 | sniper/ package extraction | Business logic → 11 modules, streamlit_app.py down to 3,827 lines | ✅ Complete |
| 9 | CMD-6→11 UI/UX enhancements | PRI gauge, signal convergence, sparklines, ticker tape, news badges, keyboard shortcuts, auto-refresh, mobile tab bar, panel manager | ✅ Complete |
| 10 | Handoff doc + CLAUDE.md overhaul | This document updated to current state | ✅ Complete |

---

## GSD SESSION PROTOCOL (step mode only)

`/gsd auto` is banned — Commander-in-loop constraint. Step mode only:
1. `/gsd` → discuss
2. `/gsd` → plan
3. `/gsd` → execute (one step at a time, confirm before each)
4. `/gsd` → verify

---

## WHAT NOT TO BUILD

- No buy/sell signals of any kind
- No auto-execution, auto-order, or broker API integration
- No `/gsd auto` mode
- No features that bypass Commander's decision in the deployment chain
- Alert systems (future P7): informational only
- No `node_modules` / webpack in `command_center/`
- Never generate or modify `.streamlit/secrets.toml`
- Never commit portfolio data (shares, cost bases, fund NAVs) to Git

---

*END OF HANDOFF — Updated Apr 3, 2026*
